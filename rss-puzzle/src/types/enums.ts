enum FormAttribute {
    PLACEHOLDER = 'placeholder',
    TYPE = 'type',
    NAME = 'name',
    ID = 'id',
    FOR = 'for',
    REQUIRED = 'required',
    ACTION = 'action',
    DISABLED = 'disabled',
    CHECKED = 'checked',
    VALUE = 'value',
    SELECTED = 'selected',
}

enum AppPage {
    LOGIN = 'login',
    START_PAGE = 'start page',
    MAIN_PAGE = 'main page',
}

enum ImageAttribute {
    SRC = 'src',
    ALT = 'alt',
}

export { FormAttribute, AppPage, ImageAttribute };
